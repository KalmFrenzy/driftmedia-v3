{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "builds": [
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "env": {},
  "routes": [
    { "src": "^/(.*)", "dest": "/$1" }
  ]
}
